# Question 5 – sync.sh

This script compares two directories, dirA and dirB, without modifying any files.

It lists files that exist only in one directory and uses the cmp command to compare files with the same name in both directories to determine whether their contents are identical or different.
